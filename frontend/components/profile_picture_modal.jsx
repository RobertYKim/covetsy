var React = require('react');

ProfilePictureModal = React.createClass({
  render: function () {
    return (
      <div className="profile-picture-modal">Profile Picture Modal</div>
    );
  }
});

module.exports = ProfilePictureModal;
