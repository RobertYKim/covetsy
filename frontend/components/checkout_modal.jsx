var React = require('react'),
    CheckoutModalActions = require('../actions/checkout_modal_actions');

var CheckoutModal = React.createClass({
  render: function () {
    return (
      <div>This is a checkout modal</div>
    );
  }
});

module.exports = CheckoutModal;
